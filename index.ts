import { Context } from 'hydrooj';

// 本插件只提供静态文件：public/fonts/* 会由 Hydro 以 /fonts/* 路径对外提供。
// 字体的启用在 系统设置 → ui-default → footer_extra_html 里加一行 CSS（见 README）。
export function apply(ctx: Context) { }
